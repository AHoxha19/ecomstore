name: ecomstore
description: Flutter project for Cross Platform and IS courses

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  google_fonts: ^3.0.1
  provider: ^6.0.3
  badges: ^2.0.3
  image_picker: ^0.8.5+3
  rxdart: ^0.27.5
  dio: ^4.0.6


  cupertino_icons: ^1.0.5
  cloud_firestore: ^3.4.9
  firebase_core: ^1.23.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1


flutter:


  uses-material-design: true

  assets:
    - images/
